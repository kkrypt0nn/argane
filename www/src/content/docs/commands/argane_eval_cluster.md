---
title: argane eval cluster
description: Evaluate a cluster and all its pod-compatible running workload from the current kubeconfig against the Kubernetes Pod Security Standards.
---

Evaluate a cluster and all its pod-compatible running workload from the current kubeconfig against the Kubernetes Pod Security Standards.

```
argane eval cluster [flags]
```

### Options

```
  -A, --all-namespaces      Evaluate across all namespaces
  -c, --context string      The context of the kubeconfig file to use, default is the currently active one
  -h, --help                help for cluster
  -k, --kubeconfig string   Absolute path to the kubeconfig file (default "$HOME/.kube/config")
  -n, --namespace string    The namespace to evaluate (default "default")
```

### Options inherited from parent commands

```
      --disable-rule strings   Disable a specific rule by ID (e.g. pss:baseline:apparmor)
  -o, --output string          Output format: stdout or json (default "stdout")
  -p, --policy string          Policy to evaluate against: baseline, restricted or any Y(A)ML file (default "restricted")
  -V, --violations             Show only rule violations
```

### SEE ALSO

* [argane eval](argane_eval.md)	 - Evaluate Kubernetes resources against Pod Security Standards.

###### Auto generated by spf13/cobra on 9-Apr-2026
